-- MDTV Configuration File
-- Centralized settings for dispatch systems and options

Config = {}

-- Example setting for enabling or disabling integration with dispatch systems
Config.DispatchIntegration = {
    cd_dispatch = true, -- Enable cd_dispatch integration
    codem_dispatch = true -- Enable codem_dispatch integration
}

-- Add other configurations as needed, like webhook URLs, case limits, etc.
